# Local-only files — never commit to the public repo
AUDIT.md
.claude/
.agents/
.DS_Store
report.html

# Personal / property documents that live in this folder but must not be published
orgcharts.pdf
Property Bible/
Binder/
quick-reference-cards.html
sgt-tour-key-labels-avery8460.html
SGT Tour Key Labels - Avery 8460.pdf
vendor-onetime-card-labels-avery8460.html
sgt-labels-new-units-727-2703.html
# Printable label sheets are generated per batch and carry unit/resident data — never deployed.
# Globbed so new ones don't need a new line here each time (both cases: git matches case-sensitively).
*labels*.pdf
*labels*.html
*Labels*.pdf
*Labels*.html

# Vendor documentation — third-party copyrighted manuals, not ours to republish
Visionline_User_Manual.pdf

# Brand assets + local tooling not part of the deployed site
Icon-*.png
Wordmark-*.png
serve.py

# Stale browser-save artifacts (extracted copies of index.html's inline JS)
script_*.js

# Word docs (reports/reconciliations) — routinely contain resident/staff PII, never publish
*.docx
~$*.doc*
